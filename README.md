# QueuedRemove

QueuedRemove is a plugin of [Deluge](http://deluge-torrent.org). It maintain a (user selected) queue, and automatically remove the torrents in the queue while free disk space go down to a threshold.
